# Python
__pycache__/
*.py[cod]
.Rhistory

# Heavy training artifacts — keep the small JSON metrics, drop everything large.
# (results/ is otherwise tracked so *_metrics.json can be committed as the record.)
results/**/*.pickle
results/**/*.pt
results/**/events.out.tfevents.*

# Local scratch
*.docx

# Trained model weights — available from the author on request, not published here
predictor/checkpoints/
predictor/checkpoints_esm1b/
predictor/checkpoints_esm2/

