# Core training + ESM-2 (fair-esm) embedding dependencies — main branch.
# torch>=2.0 is required: the model uses nn.Dropout1d (torch>=1.12) and the code
# was modernised for torch 2.x. FSDP/fairscale is no longer used.
fair-esm>=2.0.0
torch>=2.0.0
pandas>=1.5.0
scikit-learn>=1.0.2
scipy>=1.8.0
tensorboard>=2.8.0
tqdm>=4.64.0
matplotlib>=3.5.1
seaborn>=0.11.2
